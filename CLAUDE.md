# brain repo

This folder is the brain installer. If `~/.claude/brain-config.json` does not exist, brain is not installed yet: run `bash install.sh` from this folder first (one command, no prompts), then continue with whatever the user asked. After install the skills live in the brain plugin, see SKILLS.md.
